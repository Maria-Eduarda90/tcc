import styled from 'styled-components';

export const Container = styled.div `
    
`;

export const Input = styled.input `
    padding: 10px 63px;
    width: 16.06rem;
    height: 2.56rem;
    border-radius: 0.5rem;
    background: rgba(17, 76, 116, 0.1);
    color: var(--color-card);
    font-size: 0.875rem;
    font-family: 'Poppins', sans-serif;
    border: none;
    margin-top: 1rem;
`;